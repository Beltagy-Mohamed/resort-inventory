const fs = require('fs'); const content = fs.readFileSync('templates/chat/room.html', 'utf8'); const newContent = content.replace(/async function sendMessage\(\) \{[\s\S]*?headers = \{ 'Content-Type': 'application\/json', 'X-CSRFToken': CSRF_TOKEN \};\s*\}/, "function toBase64(file) { return new Promise((resolve, reject) => { const reader = new FileReader(); reader.readAsDataURL(file); reader.onload = () => resolve(reader.result); reader.onerror = error => reject(error); }); }\n\nasync function sendMessage() { try { const dummyCtx = new (window.AudioContext || window.webkitAudioContext)(); dummyCtx.resume(); }catch(e){} const input = document.getElementById('chatInput'); const content = input.value.trim(); if (!content && !selectedImageFile && !selectedFile) return; input.value = ''; input.style.height = 'auto'; let image_b64 = null; let file_b64 = null; let file_name = ''; if (selectedImageFile) image_b64 = await toBase64(selectedImageFile); if (selectedFile) { file_b64 = await toBase64(selectedFile); file_name = selectedFile.name; } const body = JSON.stringify({ content, image_b64, file_b64, file_name }); const headers = { 'Content-Type': 'application/json', 'X-CSRFToken': CSRF_TOKEN }; clearImage();"); fs.writeFileSync('templates/chat/room.html', newContent);
