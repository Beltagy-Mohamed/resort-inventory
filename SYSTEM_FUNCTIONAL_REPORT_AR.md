# التقرير الوظيفي الشامل — نظام مخزون المنتجع

تاريخ القراءة: 2026-09-13  
أساس التقرير: قراءة الملفات الحالية في `config/` و`inventory/` و`templates/` و`static/` وملفات migrations وتهيئة النشر. يصف التقرير ما تنفذه الصفحات والشفرة الموجودة الآن، لا ما كان موجوداً في إصدار قديم.

## الملخص

هذا نظام ويب داخلي باللغة العربية لإدارة مخزون منتجع أو منشأة مشابهة. مبني بـ Django، ويغطي المنتجات، الفئات والألوان والمقاسات، مخازن/فروع متعددة، جهات التعامل (مورد/عميل/جهة أخرى)، حركات الاستلام والصرف والجرد، لوحة متابعة، تنبيهات نقص المخزون، تقارير مخزون وأرباح، وتسجيل نشاطات المستخدمين.

فكرة النظام المحورية هي أن كل منتج له **رصيد إجمالي**، وله في الوقت نفسه أرصدة منفصلة داخل كل مخزن. عند إجراء حركة على مخزن محدد، يُحدَّث رصيد ذلك المخزن ثم يُعاد حساب رصيد المنتج الإجمالي من أرصدة المخازن.

## التقنية والتشغيل

| المكوّن | ما يفعله |
|---|---|
| Django 6.1 | الخلفية، الجلسات، المستخدمون، الصلاحيات، النماذج، والصفحات الخادمية |
| SQLite / PostgreSQL | SQLite محلياً، وPostgreSQL مدعوم للإنتاج عبر `DATABASE_URL` أو متغيرات `DJANGO_DB_*` |
| WhiteNoise | تقديم الملفات الثابتة من التطبيق عند التهيئة الصحيحة |
| Gunicorn + Nginx | ملفات تشغيل مرجعية على VPS |
| openpyxl | تصدير تقرير المخزون إلى Excel (`.xlsx`) |
| Chart.js من CDN | رسوم لوحة التحكم: فئات، حركة زمنية، وقيمة المخزون |

التطبيق يستخدم جلسة دخول Django. الجلسة تنتهي عند إغلاق المتصفح، ومدة cookie مضبوطة على ساعة. في الإنتاج يفعّل Cookies الآمنة، HTTPS redirect، HSTS، ومنع MIME sniffing وclickjacking عند `DJANGO_DEBUG=False`.

## تجربة المستخدم والتنقل

واجهة النظام عربية وRTL افتراضياً، بعنوان **«مخزون المنتجع»**. تحتوي كل صفحة داخلية على:

- شريط جانبي قابل للطي على الكمبيوتر.
- قائمة تنقل موبايل، وقائمة جانبية تُفتح بزر hamburger على الشاشات الصغيرة.
- بحث سريع في أعلى الصفحة ينتقل إلى قائمة المنتجات بالاسم.
- زر ثيم داكن/فاتح، وتُحفظ التفضيلات محلياً في المتصفح (`localStorage`).
- جرس تنبيهات يعرض المنتجات النافدة أو القريبة من الحد الأدنى، مع رابط مباشر للتفاصيل.
- اسم المستخدم الحالي وزر تسجيل الخروج.
- رسائل نجاح أو فشل بعد العمليات.

القوائم والجداول Responsive في ملفات CSS، وتستخدم خطوط IBM Plex Sans Arabic وأيقونات Bootstrap Icons. لا توجد حالياً واجهة اختيار لغة أو ترجمة إنجليزية عملية؛ اللغة المضبوطة عربية ومعظم النصوص مكتوبة بالعربية مباشرة.

## الدخول والمستخدمون والصلاحيات

### تسجيل الدخول

توجد صفحة دخول باسم مستخدم وكلمة مرور. لا يمكن فتح أغلب الصفحات دون تسجيل الدخول، وعند الخروج تعود الجلسة إلى صفحة الدخول.

### الإدارة العليا

الحساب `is_superuser` هو مدير النظام الفعلي. يستطيع من الواجهة:

- فتح إدارة المستخدمين والصلاحيات.
- إنشاء مستخدم موظف، تعديل بياناته، تعطيل حسابه أو حذفه (ولا تحذف الواجهة superuser).
- تعيين صلاحيات منفصلة: إدارة المنتجات، الحركات، التقارير، والحذف.
- فتح إعدادات النظام.
- عرض سجل النشاطات الكامل.

### الموظف

يمكن منحه صلاحيات مباشرة على نماذج Django، مثل إضافة/تعديل المنتجات أو إنشاء/عرض الحركات. الشريط الجانبي يخفي بعض الروابط وفق الصلاحية، ويُفحص الإذن من الخادم لعمليات عديدة مثل إضافة منتج أو إضافة حركة.

### تدقيق النشاط

يوجد `CurrentUserMiddleware` يضع المستخدم الحالي في thread-local. إشارات Django تسجّل عمليات الإضافة والتعديل والحذف للفئات والألوان والمقاسات والمنتجات والمستخدمين، كما تسجل الحركات المخزنية؛ وتربط السجل بالمستخدم إن كان الطلب من مستخدم مسجّل الدخول. صفحة السجل تعرض الوقت، المستخدم، نوع الفعل، المنتج والوصف، وتسمح للمدير بتصفيته باليوم والشهر والسنة مع pagination.

## إدارة المنتجات

### قائمة المنتجات

صفحة **المنتجات** تعرض جدولاً مقسماً إلى صفحات (10 منتجات في الصفحة). لكل صف:

- اسم المنتج والفئة.
- سعر الشراء/التكلفة وسعر البيع.
- الرصيد الإجمالي.
- حالة المخزون: متاح، منخفض، أو نافد.
- أزرار العرض والتعديل والحذف.

يوجد بحث بالاسم وفلترة حسب الحالة. الحالة تُحسب كالتالي:

- `نفد`: الكمية الإجمالية صفر.
- `منخفض`: الكمية أكبر من صفر وأقل من أو تساوي الحد الأدنى.
- `متاح`: الكمية أكبر من الحد الأدنى.

### إضافة منتج

النموذج يجمع الاسم، الفئة، اللون، المقاس، سعر التكلفة، سعر البيع، الكمية، الحد الأدنى، والوصف. يتحقق من الحقول النصية التي تحتوي مسافات فقط، ومن عدم إدخال أرقام سالبة. إذا أُدخل رصيد افتتاحي أكبر من صفر وكان يوجد مخزن، ينشئ النظام حركة استلام افتتاحية في أول مخزن موجود.

### تفاصيل وتعديل وحذف

صفحة التفاصيل تعرض هوية المنتج، حالته، الفئة واللون والمقاس والكمية والوصف وآخر عشر حركات. منها يمكن الانتقال للتعديل أو الحذف. الحذف يتطلب تأكيداً في صفحة منفصلة.

## البيانات المرجعية

### الفئات

يمكن عرض وإضافة وتعديل وحذف فئات المنتج. لكل فئة اسم ولون Hex يُستخدم في رسوم لوحة التحكم. القائمة تعرض عدد المنتجات التابعة لكل فئة.

### الألوان والمقاسات

كل منهما كتالوج بسيط: قائمة، إضافة، تعديل، وحذف. يمكن ربط المنتج بلون ومقاس اختياريين.

### المخازن والفروع

المخزن له اسم فريد، موقع/عنوان، وأمين مخزن. توجد صفحة قائمة وإضافة وتعديل. يستخدم المخزن عند كل حركة مخزنية لتحديد مكان تأثير الكمية.

### جهات التعامل

جهة التعامل قد تكون موردًا أو عميلاً/جهة صرف أو جهة أخرى، ولها اسم ومعلومات اتصال. توجد قائمة وإضافة وتعديل. عند الاستلام تظهر في النموذج الموردون فقط؛ وعند الصرف تظهر العملاء والجهات الأخرى.

## حركة المخزون

صفحة **إذن إضافة/استلام بضاعة** تستخدم `?action=IN`، وصفحة **إذن صرف بضاعة** تستخدم `?action=OUT`. يوجد أيضاً نوع `ADJUST` للجرد، لكنه لا يظهر كاختصار واضح في القائمة.

كل حركة تتضمن المنتج، المخزن، الجهة، الكمية، سعر الوحدة، والملاحظات. الحقل `transaction_type` يُفرض في الخادم من قيمة `action` في الرابط، لذلك لا يستطيع المتصفح تغيير IN إلى OUT بتعديل الحقل المخفي فقط.

يملأ JavaScript سعر الوحدة تلقائياً عند اختيار المنتج:

- الاستلام يبدأ بسعر التكلفة.
- الصرف يبدأ بسعر البيع.

منطق المعالجة يقوم بـ:

1. إنشاء/جلب رصيد المنتج في المخزن.
2. زيادة الرصيد في IN.
3. منع OUT إذا لم يكف رصيد ذلك المخزن.
4. جعل ADJUST يمثل الرصيد الفعلي الجديد للمخزن.
5. حفظ الحركة، ثم إعادة حساب كمية المنتج الإجمالية من كل أرصدة المخازن.

سجل الحركات قابل للبحث باسم المنتج أو رقمه الداخلي، والفلترة بالنوع والمخزن والشهر والسنة، ومقسم إلى صفحات من 15 حركة. الجدول يعرض المنتج، المستخدم، النوع، الجهة، المخزن، الكمية والتاريخ.

## لوحة التحكم

لوحة التحكم تعرض فترة شهر معيّن من السنة الحالية أو السنة كاملة، مع فلتر فئة. المؤشرات المعروضة:

- قيمة المخزون: التكلفة × الرصيد الإجمالي.
- عدد المنتجات والفئات وإجمالي الوحدات.
- عدد المنتجات منخفضة المخزون والنافدة.
- حركات اليوم.
- نشاط الفترة مقارنة بالفترة السابقة.
- الوحدات المستلمة والمصروفة وصافي الحركة.
- أحدث المنتجات وأحدث الحركات.
- المنتجات الأكثر نشاطاً خلال الفترة.
- قوائم منتجات منخفضة ونافدة.

كما تتضمن ثلاثة رسوم Chart.js: عدد المنتجات حسب الفئة، عدد الحركات عبر أيام/شهور الفترة، وقيمة المخزون حسب الفئة.

## التقارير

### تقرير المخزون

يعرض المنتجات مع الاسم، رقم المنتج الداخلي، الفئة، الرصيد الإجمالي، الحالة، سعر التكلفة والقيمة الإجمالية. يدعم البحث بالاسم/رقم المنتج، الفئة، المخزن، والحالة. يمكن تصديره إلى Excel بتنسيق عربي واتجاه RTL ورؤوس وألوان وحالات مخزون ملوّنة.

### جرد المخازن التفصيلي

يعرض فقط الأرصدة الموجبة، مفصّلة حسب المخزن والمنتج والكمية وسعري التكلفة والبيع، مع اختيار مخزن محدد.

### كشف حساب جهة تعامل

بعد اختيار المورد أو العميل، يعرض حركاته خلال شهر/سنة اختيارية: التاريخ، نوع الحركة، المنتج، الكمية، سعر الوحدة، الإجمالي، المخزن والملاحظات.

### تقرير الأرباح والمبيعات

يعامل كل حركة OUT باعتبارها مبيعات. يحسب لكل حركة:

`(سعر الوحدة وقت الصرف - سعر تكلفة المنتج الحالي) × الكمية`

ويعرض إجمالي المبيعات والتكلفة وصافي الربح، مع فلاتر مخزن وشهر وسنة.

## الإشعارات وحالة المخزون

جرس الإشعارات لا يحتفظ بجدول إشعارات مستقل؛ بل يحسب الحالات لحظياً من بيانات المنتجات. يعرض عدّاد منخفض + نافد، وقائمة مختصرة بحد أقصى ستة منتجات: النافدة أولاً ثم المنخفضة. هذا يمنع وجود إشعارات قديمة لا تعكس الرصيد الحالي.

## ما لم يعد موجوداً فعلياً

ميزة QR والباركود والطباعة **غير متاحة في النسخة الحالية**:

- migrations 0019–0023 أزالت QR، الباركود، كود المنتج المتسلسل، ونموذج `CodeSequence`.
- لا توجد مسارات أو صفحات فعّالة للطباعة أو QR/Barcode.
- المعرف المتاح حالياً هو `Product.id`، ويظهر في القوالب باعتباره «كود المنتج».

ملفات CSS وREADME القديمة ما زالت تذكر QR/Barcode، لكن لا ينبغي اعتبار ذلك وظيفة متاحة. ويجب تحديث الوثائق وحذف الأصول القديمة حتى لا تضلل المستخدم.

## نقاط عدم اتساق ملحوظة قبل التسليم

هذه ليست وصفاً للوظائف، لكنها ضرورية لفهم حدود النظام الحالي:

1. صفحة تفاصيل المنتج ما زالت تحاول عرض `product.price`، بينما النموذج الحالي يستخدم `cost_price` و`selling_price`. النتيجة المتوقعة في الواجهة هي سعر فارغ بجانب العملة؛ يجب عرض السعرين بوضوح.
2. تعديل المنتج يسمح بتغيير `quantity` مباشرة، ما قد يخالف أرصدة المخازن وسجل الحركات. يجب منع تعديل الكمية من نموذج المنتج أو تحويله إلى حركة جرد موثقة.
3. عند إضافة منتج برصيد افتتاحي وعدم وجود أي مخزن، يُحفظ الرصيد الإجمالي بلا حركة افتتاحية أو رصيد مخزن مقابل. يجب منع ذلك أو إنشاء المخزن الرئيسي تلقائياً.
4. منطق المخزون الذري موجود داخل transaction database، لكنه لا يقفل سجل `Stock` قبل القراءة/الكتابة. في عمليات متزامنة على نفس الرصيد يمكن فقد تحديث؛ يلزم `select_for_update()` واختبارات تزامن على PostgreSQL.
5. صفحات إدارة المخازن وجهات التعامل تتطلب تسجيل الدخول فقط في الخلفية، رغم أن القائمة تضعها تحت «الإدارة». يلزم `permission_required` صريح.
6. صفحات التقارير تتطلب تسجيل الدخول فقط في الخلفية؛ الرابط مخفي دون `view_inventorytransaction` لكن يمكن الوصول إليها مباشرة. يلزم فرض إذن من الخادم.
7. `profit_report` لا يرسل `warehouses` و`warehouse_filter` إلى القالب، لذلك فلتر المخزن في الصفحة لا يملك خيارات/لا يحفظ الاختيار. كما أن تقرير الربح يعتمد سعر تكلفة المنتج الحالي لا تكلفة تاريخية مثبتة وقت البيع.
8. كشف حساب الجهة يعرّف `warehouse_filter` لكنه لا يستخدمه. الإجمالي في القالب يستخدم `widthratio` وقد يفقد الكسور العشرية؛ الأفضل حسابه Decimal في الاستعلام أو قالب مخصص.
9. يوجد تعارض بين README القديم الذي يقول إن `InventoryService` مفقود وبين الملف الموجود فعلاً حالياً. يجب تحديث README. كذلك توجد ملفات قديمة/مؤقتة مثل `.bak` و`.tmp` وCSS يحتوي محارف NUL؛ يلزم تنظيفها بعد التحقق.
10. `makemigrations --check --dry-run` ما زال يطلب migration 0024 لتباين خيارات الترجمة في `ActivityLog.action` و`InventoryTransaction.transaction_type`. لا تنشر قبل إنشاء ومراجعة هذا migration.

## نتيجة الفحص الحالي

| الفحص | النتيجة |
|---|---|
| `python manage.py check` | ناجح، دون أخطاء نظامية |
| `python manage.py test` | ناجح: 5 اختبارات موجودة |
| `python manage.py makemigrations --check --dry-run` | غير نظيف: يقترح migration 0024 |

الاختبارات الحالية تفحص WhiteNoise، دعم `DATABASE_URL`، وحصر إدارة المستخدمين في superuser وإضافة صلاحية منتج. لا تغطي منطق IN/OUT/ADJUST، المخازن المتعددة، التزامن، أو التقارير؛ وهذه يجب إضافتها قبل اعتماد إنتاجي.
